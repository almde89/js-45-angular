(function (doc) {
  var app = angular.module('meusServicos', ['ngMessages'])
    .factory('recursoFoto', ['$resource', function ($resource) {
      return {

      };
    }]);// criação do módulo
})(document)
